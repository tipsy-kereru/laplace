---
description: Stop an active Laplace loop safely — clears state, records cancellation, keeps branches
argument-hint: "[issue-id]"
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---

Read and follow `${CLAUDE_PLUGIN_ROOT}/skills/laplace-cancel/SKILL.md` in full, then execute it with arguments: $ARGUMENTS
